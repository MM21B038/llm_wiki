# Lessons Learned

- Memory system files must be read at start of each task for context.
- Commands that work: mkdir, cat, ls, python3, git.
- MLflow database found at mlflow.db — can query with SQLite.
- `.ipynb` files are Jupyter notebooks — can extract with jq or python.

*This file grows as we learn.*
