# Summarized Security Assessment Report

**Target:** `admission.bhu.ac.in`  
**Date:** [Not provided in source data – assume current]  
**Scope:** Network reconnaissance (port scanning) of the target domain and its resolved IP addresses.

---

## Executive Summary

A reconnaissance scan was performed against the target domain `admission.bhu.ac.in` (resolving to two IP addresses). One IP (`14.139.41.206`) showed all 1000 scanned TCP ports in a filtered state, indicating a firewall or network‑level access control blocking inbound probes. The secondary IP (`103.55.72.206`) exposed four open TCP ports: HTTP (80), HTTPS (443), XMPP (8010), and an unknown service (8020). No application‑level vulnerabilities were identified in this assessment; the report focuses on the attack surface exposed by these open ports.

---

## Assets

| Asset / Target | IP Address | Notes |
|----------------|------------|-------|
| `admission.bhu.ac.in` | **14.139.41.206** | All 1000 scanned ports filtered (likely behind firewall) |
| `admission.bhu.ac.in` (secondary) | **103.55.72.206** | Four open TCP ports identified (see below) |

**Evidence:**  
- DNS resolution confirmed both IPs for the domain.  
- Nmap scan results provided.

---

## Open Ports & Services (103.55.72.206)

| Port (TCP) | Service | Evidence |
|------------|---------|----------|
| 80 | HTTP | `Open 103.55.72.206:80` |
| 443 | HTTPS | `Open 103.55.72.206:443` |
| 8010 | XMPP (Extensible Messaging and Presence Protocol) | `Open 103.55.72.206:8010` |
| 8020 | `intu-ec-svcdisc` (Intel‑based service discovery) | `Open 103.55.72.206:8020` |

**Note:** The services on ports 8010 and 8020 may require further investigation to confirm exact software versions and configurations.

---

## Observations

- **Filtered ports on 14.139.41.206** – This IP likely serves as the primary entry point but does not reveal any open ports from external scanning. It may still host web services behind a load balancer or firewall that only respond to specific allowed sources.
- **Open ports on 103.55.72.206** – The presence of XMPP on port 8010 and an Intel‑related service on 8020 suggests auxiliary services that could expand the attack surface if misconfigured or outdated.
- **No vulnerabilities were identified** in the scope of this port scan. No running service versions or banners were collected; deeper enumeration (e.g., HTTP header analysis, SSL/TLS checks, version detection) would be required to assess software‑level weaknesses.

---

## Recommendations

1. **Profile exposed services** – Perform version detection and banner grabbing on ports 80, 443, 8010, and 8020 to identify software and patch levels.
2. **Review firewall rules** – Confirm that port 80/443 (web) are intended to be publicly accessible. Ports 8010 and 8020 should be restricted to trusted sources if not required externally.
3. **Investigate the “filtered” IP** – Ensure that 14.139.41.206 is not silently dropping legitimate traffic; consider whitelisting test scanning IPs for internal assessments.
4. **Apply least‑access principle** – Disable or firewall any service that does not require public exposure.

---

**Disclaimer:** This report is based solely on the provided reconnaissance data. No application‑layer, web, or vulnerability scanning was performed in this assessment. The findings represent a network‑level attack surface overview.